


---= DONE =---

2026-09-07
- give options how to add dynamic text effects for the text in the text frames. for example https://reactbits.dev/
- confirm if ffmpeg hardware transcoding on synology nas ds918+ is working as gui shows "Quick Sync unavailable · CPU fallback"
- make compact detailed slide list zoom to go 500% (now 180%)
- in compact view add symbol on slide to open preview popup (in bottom right corner), and add drag and drop functionality (if you click on the middle of the slide) to change the order/location of the slide
- give options to better use the available size of the detailed slide row to show all information
- if there are 2 movies back to back (after each other) do not start the audio between the movies (during the transition)

2026-09-11
- two movies back to back: do not start audio between the movies (during the transition). Previously the second film's original audio faded in across the handoff (afade-in over the incoming transition's length starting at the film's first frame) and the first film's audio faded out early; worse, the music bed leaked into the crossfade because the outgoing duck envelope ramped up while the incoming one ramped down (product ~25% mid-transition). **Now:** when two videos are adjacent, the outgoing film's sound runs at full level until the cut (no fade-out) and the incoming film's sound starts at full level with its first held frame after the transition (no fade-in) — nothing starts or lingers inside the transition; the music-bed duck for the outgoing film stays at zero through the transition and only releases when the next film's own audio starts (release = next start instead of hold end), so the multiplied envelopes can no longer leak. Movies surrounded by pictures keep their fades unchanged. Two new compose-level tests assert the exact chains (no afade on either movie chain, envelope release at starts[next]) and the photo-neighbour contrast — suite 306/306.
- better use of the detailed slide row's size (options doc: docs/detailed-row-density-options.md; package 0+1+2+3 chosen). **Found while measuring:** the row rendered 8 cells in a 7-track grid — the up/down/remove buttons wrapped onto an implicit second line under the checkbox and the movie-audio box (min-width 125px) overflowed its 80px track; a since-the-beginning layout bug and the main space waster. **New layout (5 tracks):** select · thumbnail · info · duration+transition · actions. **Ken Burns box smaller:** the 150px EFFECT column is gone entirely — the effect is now a motion chip on the thumbnail's bottom-left corner with short labels ("Zoom in", "Pan left", "Original"; full names stay in the bulk tool), clicking it opens a compact picker popover with all options. **Duration** moved into the transition cell as its first control (76px stepper; last slide shows duration + "End of story"). **Movie audio** became an inline select + hint inside the info column, rendered for videos only. **Settings chips** (second line, only when non-default): "Text hidden" restores the caption with one click; the trim chip shows the kept length and opens the trim editor. GL params/easing/reverse stay in the transition cell's summary line; inactive look/crop chips shrink to their icon. **Honesty fix:** the hardcoded fake "6000 × 4000 · JPG" meta is replaced by a real "photo"/"video" label. Rows drop from ~2 lines to one 82px line and the name/transition columns gain ~180px at 1280px screens; ≤1100px and ≤760px breakpoints rebuilt for the new cells. Frontend build green.
- in the compact view of the detailed slide list: add a preview symbol on each slide (bottom-right corner) and make drag & drop from the middle of the slide work for reordering. Every compact card now carries a round ZoomIn button in its bottom-right corner (same geometry as the delete button in the top-right; on title frames it sits one slot left so the text-frame pencil keeps its corner) which opens the per-slide preview lightbox — clicking the thumbnail itself still opens it too. Drag & drop reorder was rewritten to be reliable: the card's dragstart now carries a dataTransfer payload with `effectAllowed: move` (Firefox silently refuses to start a drag without a payload), and `MediaThumb`'s img/video elements get `draggable=false` so the picture can never hijack the gesture with its native image drag — grabbing the middle of the slide now drags the card, with the whole card as the drag ghost. The existing drop logic is untouched: dropping on another card moves the dragged slide there, and a multi-selection moves as one group.
- make the compact detailed slide list zoom go to 500% (was capped at 160%). The Compact thumbnail grid's zoom slider and its zoom-in button now stop at 5.0 instead of 1.6 (zoom-out unchanged at 60%), so `--compactSize` can scale thumbnails from 88 px up to 440 px columns. The compact slider was also widened from 72 px to 120 px so its 0.1 steps stay draggable across the much larger 0.6-5.0 range; the +/- buttons still nudge by 0.2 and the percentage readout rounds as before.
- implement Option B: VA-API hardware encoding (follow-up to the QSV/DS918+ findings in docs/hardware-transcoding-ds918plus.md). The DS918+'s Gen9 iGPU can never run h264_qsv (the image ships only the oneVPL runtime, Gen12+), but its H.264 encode hardware is reachable through VA-API's low-power VDENC entrypoint — confirmed on-device (vainfo lists VAEntrypointEncSliceLP only). **Backend:** new `Renderer.vaapi_encodable()` probe mirrors the QSV one (find first /dev/dri/renderD*, 12-frame test encode, tries `-low_power 1` first and remembers the working mode); warmed at startup, read non-blockingly by /api/health as `capabilities.vaapi`. New `select_encoder()` decides every render: `Auto · Quick Sync` walks QSV → VA-API → CPU, new explicit `Hardware · VAAPI` walks VA-API → CPU, `Intel Quick Sync`/`CPU · x264` unchanged; stored projects with the legacy Auto label get the new chain automatically. Hold and transition FFmpeg commands gain `-vaapi_device` + a `,hwupload` hop before `[vout]`; VA-API encode args carry bitrate/bufsize and `-low_power 1` only when the probe said the host needs it (no `-pix_fmt`, frames arrive as GPU surfaces); segment preparation deliberately stays on `libx264 -crf 18` quality intermediates; concat/final mux are stream-copy. Mid-render hardware failure retries the same composition on CPU (device, hwupload, -low_power stripped; -preset medium -pix_fmt yuv420p added) — a broken driver can never fail a job. **Frontend:** checklist line shows a green `Hardware encoding available · VAAPI` when the probe succeeds (replacing the warning only if QSV is also absent), Encoder dropdown gained `Hardware · VAAPI`, render-time estimates treat it as hardware speed. **Tests:** 10 new (probe off/nodes, low-power preference + memory, fallback to normal mode, error survival, capabilities without subprocess, auto chain, explicit degradation, full-compose argv carrying `-vaapi_device`/`hwupload`/`-low_power 1`, CPU retry argv) — suite 304/304. Net effect on the DS918+ after pulling: health reports `"quickSync": false, "vaapi": true` and Auto renders use the iGPU.
- in the picture editor - cut and drop popup window I do not see the original picture on the left, also check if cut and drop functionality is working, as I cannot drag a rectangle. the filter do work. · **Root cause found: a mount deadlock in the Cut & crop tab.** The whole crop stage (picture, crop frame, handles) is only rendered once the stage box is sized; the box is sized from the picture's intrinsic dimensions; and those dimensions were only reported by the media element's load event — which lived *inside* the stage that never rendered. Nothing could ever mount, so the left side stayed empty and there was no rectangle to drag, while the Filters tab (which renders its stage picture directly) kept working — exactly the reported symptoms. **Fix:** the intrinsic size is now measured off-DOM (`new Image()` for photos, a `preload="metadata"` probe element for movies) before the box needs it, with the visible media's own load handlers kept as a redundant second measurement. **Drag chain reviewed end-to-end while fixing:** pointerdown on the frame/handles stores the start rectangle, window-level pointermove/up do the math against the stage box's live rect (aspect presets honoured on corner drags with an aspect lock, minimum crop size clamped, never past the opposite edge), pointer-up publishes to the item, clicking outside the frame recentres it, Backspace undoes lasso points — all sound once the stage mounts. Requesting verification on the NAS after pulling this build.
- is it possible also to show the detailed settings of the selected to transition in the detailed slide list? · yes — implemented as the hybrid option (advice in `docs/transition-details-detailed-list.md`). **(1) Shared inspector bar:** the filmstrip's transition-inspector JSX was extracted into a `TransitionInspector` component and is now rendered in **both** views — above the overall timeline (as before) and **above the detailed slide list's rows** whenever transitions are selected: name chip, duration stepper, GL parameter sliders (when the selected transition is GL), easing select, reverse toggle and Clear, editing all selected transitions at once. **(2) Always-visible ⚙:** the per-row settings button in the transition cell no longer hides until a value is non-default — a plain Fade row now also offers easing/reverse/GL-params editing. **(3) At-a-glance summary:** a tiny line under the cell (e.g. `cubic · reverse · 3 params`, only when something is non-default) shows the detailed settings without opening anything. No backend involvement; build clean.
- make default setting for men burns effect "none" · the last remaining Ken Burns default is gone. New photos already entered the storyline with effect "None" (the 2026-09-03 fix), but the **PHOTO SELECTION dropdown in the bulk tools still pre-selected "Ken Burns · Zoom in"** with a static "Apply Ken Burns" button — one careless click applied motion to every photo. The dropdown now opens on **"None"**, and the button is labelled **"Apply effect"** with a tooltip saying what it applies and that "None" removes the Ken Burns motion (a handy reset). Randomize actions are unchanged: they pick a random Ken Burns deliberately, and "Randomize all" keeps randomizing effects as before.
- if text on slide is disable, completely hide all the setting for the text on slide (at the moment only the text inside the text box is crossed out). hide the text box and the text transition in the storyline and in the detailed list · **Storyline:** when "text on slide" is off, the slide's whole caption bar — text input, appear transition, timing handles, disappear transition — is no longer rendered in the text lane (it was a dashed, struck-through ghost before); the lane is simply empty. **Detailed slide list:** the eye toggle stays visible in its amber "hidden" state as the switch back on, but the text input, the appear/disappear transition chips and the "On picture / New frame" select are hidden completely; an open appear/disappear popover closes too (and can no longer be opened). The dead struck-through styling was removed. Re-enabling: the eye on the clip in the filmstrip (when the slide has text) or the eye in the detailed list restores the caption with its text and timing intact — nothing was deleted, only hidden.
- add option to select/deselect all slides in 1 click. make option available in "overall time line" en in "detailed slide list" · a shared **select-all control** now sits in both requested places: in the **OVERALL TIMELINE head** (between the title and the Lines/zoom controls) and in the **detailed slide list's column head** (a new first head cell, with the grid gaining a 20 px column so MEDIA stays aligned). One click selects every storyline item (photos, videos and text frames) or, when everything is already selected, clears the selection — the checkbox shows the state, the label flips All ↔ None, and while only part of the storyline is selected it shows the count (`7/12`) with the exact numbers on hover. Disabled (dimmed) when the storyline is empty. It drives the same `selectedIds` every bulk tool uses (Ken Burns, filters, move-to-position, "Randomize selected", delete), so the count is consistent everywhere; per-item checkboxes, shift-click ranges and transition-cell selections keep working unchanged.
- next to "Randomize all" transitions also provide new option to randomized transitions between selected slides "randomized selected" · new **"Randomize selected"** button directly next to "Randomize all" in the storyline bar. It randomizes only the transitions that lead **between neighbouring selected slides**: a transition belongs to the clip it leaves, so a pair counts only when the clip and its neighbour are both in the slide selection — a selection with gaps leaves the gaps' transitions untouched, and the last slide of the storyline never gets one. It draws from the same RANDOM SOURCE select and honours the "GL params" checkbox (fresh parameters per randomized GL transition when set), and the toast reports exactly what happened: `Randomized N transitions between M selected slides · <source>` (+ "· GL parameters randomized"). Clear messages guide the edge cases: fewer than two slides selected, or a non-adjacent selection with no shared transitions. The existing selection-"Random" in the bulk tools keeps working on the transition-cell selection — the two complement each other: slides-selection (this button) vs transition-selection (that one).
- let the user decide if the parameters of the gl transitions also need to be randomized when user wants to randomized all transitions · implemented with options in `docs/randomize-gl-params.md` (O1 + O4). **The decision:** a persisted **"GL params" checkbox** in the RANDOM SOURCE group of the bulk bar (localStorage, survives reloads; greyed for the xfade-only pool) governs **both** random actions — "Randomize all" and the selection "Random" in the bulk tools — and both confirm "+ GL parameters randomized" in the toast. Before this, both actions silently reset every GL pick's parameters to registry defaults. **The draw:** `randomGLParams()` in `src/transitionControls.tsx` reuses exactly the bounds the parameter sliders show (registry min/max/step, else the same derived ranges), draws numerics uniformly on the step grid, colours as `#rrggbb`, omits text params so defaults apply; each randomized GL transition gets its own independent set; the renderer is untouched (params already pass through). **Bonus (O4):** a **"GL params" button** next to the selection "Random" re-rolls only the parameters of the GL transitions already in the storyline (selection or all) without changing the transition names — "keep the plan, surprise me with the settings" — with a clear message when there is nothing to re-roll. Verified with a 300-draw simulation against the real registry (all numerics in bounds, colours well-formed, no invented names); frontend build clean. Options considered and recorded: folding the choice into the source dropdown (rejected: conflates two decisions), split-button popover (hidden state, covers one button only).
- is it possible to show a movie strip example / movie time line in the movie editor pop-up · yes — the editor's trim strip now shows **ten real frames of the movie** laid edge-to-edge under the existing kept/cut shading, playhead and IN/OUT handles, so the strip *is* the movie timeline you drag (options in `docs/movie-editor-filmstrip.md`; chosen: hybrid source + strip-background placement, 10 cells). **Source, hybrid:** the browser first grabs the frames itself — one hidden video seeks the ten slice centres and composites a sprite on a canvas, costing only the streamed bytes around the seek points; when the browser cannot decode the file (camera Motion-JPEG AVI and friends) the editor falls back to a **backend FFmpeg filmstrip**: `GET /api/media/filmstrip` renders one small cached JPEG (one FFmpeg run, ten fast `-ss` seeks + `hstack`, declared-muxer temp + atomic replace — the transition-preview lesson applied) cached on the config volume keyed by root+path+size+mtime+count+width, so it is generated once per file and regenerates when the file changes; only if both paths fail do the original colour bars stay (dimmed). Clicking anywhere on the strip still seeks exactly as before, and trimming never reshuffles the frames mid-drag (the strip always shows the whole file). **Tests:** `backend/tests/test_filmstrips.py` — 8 cases incl. command shape (ten `-ss` seeks, `hstack=inputs=10`, `-f mjpeg` on the `.part` temp), seek timestamps spanning the whole file, cache hit, mtime invalidation, refused missing/empty/non-video files, and probe-failure-leaves-nothing-cached, against ffmpeg/ffprobe stubs that keep the muxer guard; full suite 294/294 green, frontend build clean. Hover-scrub (YouTube-style floating frame from a bigger sprite) is documented as the natural later upgrade — it only needs more cells from the same machinery.
- give option if it is possible to use local file from device that is using the gui as well. · yes — implemented as an upload flow (advice + decisions in `docs/local-device-media.md`): the render happens on the NAS, so a local file is uploaded once and then behaves exactly like a mounted file. **Storage:** a dedicated `/uploads` volume (`compose.yaml`: `${UPLOADS_PATH:-./data/uploads}:/uploads` + `UPLOADS_DIR=/uploads`; without the env, dev/bare checkouts fall back to `<config>/uploads`), per-file cap `UPLOAD_MAX_MB` (default 4096 MB). **Backend:** new `backend/app/uploads.py` + `POST /api/media/upload` — filenames sanitised with the project-file rules and de-duplicated (`beach.jpg` → `beach-2.jpg`), extension allowlist (photos & movies), streamed to disk under a size cap, then **verified with ffprobe** (a text file renamed `.mp4` is rejected with the reason; rejected files never stay on disk); the `uploads` root joined `media_roots`, so browsing, streaming/thumbnails, cropdetect, probe and rendering accept uploaded files with zero pipeline changes. **GUI:** an *uploads* location plus an **"Upload from this device"** button in the media picker (native camera-roll picker on phones/tablets; the uploads listing refreshes when a batch finishes), **drag & drop** from the desktop onto the storyline (dashed overlay hint), and a bottom-right **upload tray** with per-file progress in MB, cancel per file and clear-on-finish — completed files are added to the storyline immediately through the same code path as mounted files. **Caveats documented:** HEIC is rejected with a clear reason (browsers cannot thumbnail it and FFmpeg cannot decode it — use iPhone "Most Compatible"), projects referencing `/uploads/…` are per-installation like `/photos/…`, and the app keeps its no-auth LAN posture (uploads are the first browser-write path, mitigated by volume isolation, allowlist, size cap and sanitising). **Tests:** `backend/tests/test_uploads.py` — 10 cases incl. sanitising, dedupe, cap, empty and probe-failure-leaves-no-trace against an ffprobe stub that refuses unknown files like the real binary; suite 286/286 green, frontend build clean. **Not yet (deliberate):** audio uploads for soundtracks, chunked/resumable movies, zip import, per-file delete from the uploads root.
- browse all effects does not show the examples of the transitions message "Preview could not be rendered", "0/191 cached" · root cause found and proven with a real FFmpeg binary: the preview cache wrote every example clip to a temporary name ending in `.part` (`fade.mp4.part`), and FFmpeg picks its output muxer from the file extension — `.part` matches none, so every one of the 191 renders failed at startup ("Unable to choose an output format for 'fade.mp4.part'") before encoding a single frame; the manifest recorded `failed` for all of them and `ensure()` refuses to retry failed entries, so even the gallery's "Render all N missing" button could never heal it. The tests never caught it because the stub FFmpeg fabricated output files whatever their name. **Fix:** the render command now declares the muxer explicitly (`-f mp4` — the remedy FFmpeg's own error message recommends, atomic temp-file replace kept), and `CACHE_VERSION` bumped 1→2 so every deployment forgets the poisoned `failed` records automatically and the gallery offers "Render all 191 missing" — one click rebuilds the whole catalogue with the fixed command. **Guarded:** the test stub now refuses muxer-less output names exactly like real FFmpeg (plus a guard test proving it), the command shape is asserted (`-f mp4` on the `.part` temp), and manifest-version invalidation has its own test; full suite 276/276 green. **GUI surfacing:** failed tiles in the picker and gallery carry a red `failed` flag whose tooltip shows the exact recorded FFmpeg error, the gallery detail panel prints "Preview could not be rendered: <reason>" for the focused transition, and both footers show an "⚠ N failed" counter with the last error on hover — this diagnosis would have been readable straight from the browser. Verified end-to-end against a real FFmpeg: clips that failed before now render, `failed` 191 → 0. Full write-up incl. reproduction: `docs/transition-preview-fix.md`.
- it should be possible to edit existing text frames. give options how to implement this and access from gui (e.g. right bottom symbol to call up the text frame editor, with the correct information) · the editor already handled existing frames correctly (opens with that frame's own text, font, colours A/B + colour-change transition, caption position and duration; Cancel restores the pre-edit values — only new frames are discarded) — what was missing was discoverable access, so the editor now has four visible ways in, all prefilled with the frame's current data: **(1)** a ✎ symbol in the **bottom-right corner of every text-frame card** — round pencil button in the compact grid, a green "✎ Edit" badge on the thumb in the detailed slide list (same spot as the 🔍 on filmstrip clips, mirroring the Filter/Crop badges photos already wear) and a ✎ on title clips in the overall timeline; each tooltip reads the frame's real data (`Edit text frame · "Wedding" · 6s`); **(2)** a small ✎ **inside the caption bar** of a title's text lane on the text timing strip, next to the appear/disappear handles; **(3)** the **story preview popup** no longer skips text frames: a title renders as the frame itself (background colours with the A→B change looping, caption at its position in the real font/weight/colour), ←/→ and the left/right click halves step through frames in storyline order, the counter includes them, and the header shows `TEXT FRAME` with an **Edit frame** button beside the movies' **Cut** — the editor opens stacked on top of the preview (same z-index treatment as the movie/look editors) and the preview underneath updates live while editing; **(4)** the labelled **Edit frame** button in the detailed list stays, now with a summarising tooltip, and the hidden double-click on a filmstrip title clip still works. Card bodies (compact grid, detailed list, filmstrip) now open the **preview** on click, exactly like photos and movies, so click no longer silently means edit; drag-to-move is untouched. Options considered and skipped for now: a right-click context menu (Edit/Duplicate/Delete) — a new interaction pattern; noted in `docs/text-frame-editing.md` together with the alternatives. Front-end only (`src/App.tsx`, `src/styles.css`): every access funnels through the one existing `setEditingTextFrame(id)` entry, no data-model or backend change.

2026-09-06
- "load project" and "save project" button should call up browse popup so user can select path and filename where to save/load · both buttons now open a picker built on the same browse dialog the Output pane already uses. **Save project** asks where the file goes: the volume (`/output` — the only writable mount; `/photos`, `/videos` and `/music` are listed greyed out and marked *read-only*, so it is obvious why they cannot be chosen), then the folder (the `/output` root became browsable for this — until now only the media mounts could be opened, and missing folders are created on save), then the filename, shown as `Portugal summer` + `.slideshow.json` so the name comes from the project while the type stays fixed. A destination line reads `→ /output/trip/Portugal summer.slideshow.json` while you type, and if that file is already there the row turns into **File exists — replace it?** with a *Replace file* button instead of overwriting quietly (the backend answers 409 `project_exists` with the path — the same handshake the render uses for an existing MP4). Saving writes the file **and** the SQLite row, because the database is what restores the editor after a refresh, feeds autosave, keeps revisions and drives the render queue, while the file is a portable copy you can back up or hand to somebody else: the notification says `Project saved to /output/trip/… · and to SQLite`, or says honestly when the file is there but SQLite needs another save. The last destination is remembered, so saving twice in a row is one click. **Load project** gained a second tab: *Saved in SQLite* (unchanged, with the per-row trash can and *Delete all*) and *Browse files*, which lists project files on all four mounts — folders plus `.slideshow.json` (and plain `.json`, so a file renamed by hand still opens) cards with the project name, size and date — and opens one with a click. Loading a file runs the snapshot through the same code path a database row uses and then stores it as a **fresh** row, since the file is a copy rather than the project being edited. Writes are atomic (a temporary file in the destination folder + `os.replace`), so the NAS never holds a half-written project, and the JSON is pretty-printed and keeps media paths as `/photos/…` UI paths so a project file is portable between installations. Nothing outside the mounts can be reached: a filename keeps only its last component (`../../evil` → `evil.slideshow.json` inside the chosen folder, `/etc/passwd` → `passwd.slideshow.json`), illegal characters become a dash, edge dots and dashes are trimmed, the stem is capped at 80 characters, `..` in the folder is refused with 400, saving to a read-only mount with 403, and a file that is not a project (wrong extension, empty, broken JSON, no media list, or over 32 MB) is reported as *not a project file* rather than opened. New `backend/app/project_files.py` + `GET`/`POST /api/project-files` + a `projects=true` flag on `/api/media/browse`; browser half in `src/projectFiles.ts` / `src/ProjectFileBrowser.tsx`. The filename rules exist on both sides and are mirrored statement for statement — one of the 34 new backend tests reads `src/projectFiles.ts` and fails if the two ever disagree (`docs/project-files.md`).
- in output pane the filename should be the same as the project name on the top (and visa versa) · they are one name now: typing in the project name at the top renames the file in the Output pane, and typing in the Output pane's **Filename** renames the project — the field says `same as the project name` so the link is visible. A project name is also the caption on your pictures, so it stays free text (accents, commas, an ampersand, a colon); only the twin that reaches the disk goes through `safeFilename()` in the new `src/projectName.ts`, which replaces the characters no filesystem accepts (`\ / : * ? " < > |` and control characters) with a dash, drops edge dots, spaces and dashes (Windows removes them silently, which would make the saved file impossible to find) and caps the name at 80 characters. A name that is already filename-safe comes out character for character identical, so both fields show the same text. The filename is settled on blur (sanitising on every keystroke would swallow a colon mid-word) **and** sanitised again in the saved snapshot, so a render started straight after typing can never be handed a name the filesystem would reject; the backend keeps taking `Path(filename).stem`, so a stray separator cannot leave the output folder either. Loading an older project: a filename this app invented itself (`slideshow`, `movie`, `Portugal-summer`, `Untitled`, empty) now inherits the project name — that was the real annoyance — while a filename you chose yourself is kept, with a one-line note in the Output pane saying the first edit links them again. "New project" starts as `Untitled` / `Untitled.mp4` and a fresh editor as `Portugal summer` / `Portugal summer.mp4`.
- on the picture preview popup add options to cut and crop parts of the picture · built as you chose: **all four** cutting tools, user-selectable, in a **second tab** of the *Edit picture* popup (**Filters & effects** | **Cut & crop**), for **photos and movies**, and the cropped picture shows **everywhere** — storyline, compact grid, detailed slide list, filmstrip, lightbox, slideshow Preview and the MP4 (`docs/picture-crop.md`). The tools: a **crop frame** with eight handles (drag it anywhere, click outside to recentre, aspect presets Free / Original / Frame 16:9 / 16:9 / 4:3 / 3:2 / 1:1 / 9:16 that keep the centre), **straighten** ±15° with the picture zooming so no turned corner ever shows and a rule-of-thirds grid, **cut out** — click a polygon around a person or object, drag the points, Backspace undoes, and the hole is filled with a blurred copy of the same picture with an adjustable feather — and **black bars**, one click that lets FFmpeg's `cropdetect` measure the real file and propose the rectangle. Under the tools a 16:9 "In the slideshow frame" preview shows the result exactly as the render frames it: the cropped picture over a blurred, darkened copy of itself. Nothing is written to your files: a cut is one optional `crop` value on the media item (`rect` fractions, `degrees`, `lasso` polygon, `feather`), stored with the project like `rotation` and the picture looks, so the read-only `/photos` and `/videos` mounts stay untouched and **Reset crop** really restores the original. The same numbers drive both sides — canvas and CSS in the browser (`src/pictureCrop.ts`), FFmpeg filters in `backend/app/picture_crop.py`, inserted **before** the frame fit so the letterbox backdrop, Ken Burns, the picture look and the caption all see the cropped picture. Straightening and cropping are a single resample (`rotate` keeps the canvas, one even-truncated `crop` takes the inscribed rectangle), the inscribed-rectangle formula is identical on both sides and is tested against a brute-force geometric search and against the FFmpeg expressions the module actually emits (evaluated at four source sizes, within 2 px, always even, only `iw`/`ih` — no probing). The cut-out becomes a 512 px greyscale PGM mask fed in as a bounded still stream and composited with `alphamerge` + `overlay` (not `maskedmerge`, which corrupts chroma on yuv420p), with the blurred fill made on a 640 px copy for speed. Thumbnails wear the crop through one small cached canvas copy per clip (360 / 720 / 1600 px tiers with 12 / 8 / 24 MB budgets); a **playing** movie cannot be repainted, so it wears a live CSS sprite instead — the window box gets the crop's aspect and the video inside is oversized and offset until the rectangle lines up, straightening included, at full resolution. Crop comes first and the filter second everywhere, so a cropped + filtered clip previews and renders in that order. Known limits, stated in the editor: the cut-out hole shows on a movie's thumbnail, in the result preview and in the render but not on the *playing* stage, the browser's fill is a downscaled copy rather than a real Gaussian blur (Safari has no `ctx.filter`), and `cropdetect` only finds near-black borders — its proposal is never applied until you click **Use this crop**. 69 new backend tests (parity, geometry, masks, graph, cropdetect, renderer ordering, endpoint); a full-frame crop with nothing straightened emits no filter at all, so untouched projects render byte-identically.
- on the picture preview popup add options to edit / change the pictures by adding effect and filters (https://www.photofilters.com/), for example: make picture black and white · advice first in `docs/picture-filters.md`, then built as you chose: a **Filters** button in the picture preview opens a stacked *Edit picture* popup (the movie Cut/Crop pattern) with 19 looks in five groups — Mono / Noir / Silver · Vivid / Faded / Warm / Cool · Sepia / Vintage / Polaroid / Sunset / Cinema / Duotone / Infrared · Soft focus / Vignette / Sharpen / Pixelate / Negative, plus Original. Every chip is a live thumbnail of *this* picture (one small canvas copy feeds them all, so a 24 MP photo is not decoded 19 times), each preset has an intensity slider (0-100 %) and six manual sliders sit underneath (brightness, contrast, saturation, warmth, vignette, softness); holding **Space** compares against the original. Nothing is written to your files: the look is three values (`filter`, `filterAmount`, `filterAdjust`) on the media item, so the read-only `/photos` and `/videos` mounts stay untouched and every filter can be reset. The same numbers drive both sides — CSS in the browser (`src/pictureFilters.ts`) and an FFmpeg chain in `backend/app/picture_filters.py`, inserted after the Ken Burns zoom and before the caption so the text stays crisp — which means the MP4, the low-res preview and the accurate transition preview all match (131 looks compared value-by-value between the two implementations: identical; 35 new backend tests). The look also shows on the storyline / grid / detailed-list thumbnails and in the slideshow Preview, the lightbox button reads `Mono · 80 %` when a picture is filtered, the detailed slide list has its own per-picture Filters chip, and the PHOTO SELECTION pane can apply a filter to the selection (or to every photo and movie when nothing is selected). Movies are filtered too. Known limits: a movie's stage keeps playing the real file, so Pixelate shows in its chip and in the render but not on that stage, and Sharpen previews as a small contrast bump because CSS has no sharpen — both are stated in the editor. Space compares with the original on pictures; movies get a **Show original** button because Space belongs to their player.
- on the picture preview popup move the next / previous buttons inside the picture and make the active area bigger (complete right side of picture: go to next; left side of the picture: go to previous) · the arrows now sit on the picture itself and each half of it is one big button: left half = previous, right half = next (hovering darkens that half so you can see what you are about to click). Movies keep their middle column and the bottom 56 px free, so the native video controls and click-to-pause keep working; audio previews have no picture and no overlay. The ← / → keys, the position badge, rotate / cut / delete in the header are unchanged.
- for the time ruler indicate the time in hour:minutes:seconds (h:mm:ss) instead of only seconds · new `formatTimecode()` in `src/time.ts`; the ruler below every storyline row now prints its ticks, the row-end badge and the soundtrack length as h:mm:ss (hovering a tick still shows the exact number of seconds). The zoom badge moved just below the middle tick so the longer timecodes are never covered, and the last tick only draws its mark because the badge next to it already shows that time.

2026-09-05
- add possibility to enter a default time for all slides (put it net to "transition default") · "Slide default" stepper + "Apply to all" in the bulk bar; used for every new photo/text frame, saved with the project; videos keep their native length
- add favicon
- add enable/disable function for text on picture. advise on gui options for storyline and list with photos (see docs/text-on-picture-toggle.md)
- add cut and crop function for movies (same functionality as for the soundtracks)
- show transition symbol also in the detailed slide list (same as storyline)
- in detailed slide list make box for effect smaller (fit to largest text)
- advise on better gui for the selection of the transition in the detailed slide list as the list is now too long
- make new popup window to show small examples of all possible transitions. remember / save the examples so you only need to generate them 1 time. give options for gui
- move the complete pane with "PHOTO SELECTION - All photos" and "TRANSITION DEFAULT" just above the storyline (instead of below the storyline)
- include "random text transition" button in "PHOTO SELECTION - All photos" pane
- in project header near the "load project", "clear all", "save project" buttons give also indication if generation is ongoing (including progress and estimated time to finish)
- in generation and output pane put "estimated time to generate", estimated file size, ESTIMATED TOTAL SLIDESHOW TIME
- Transition preview popup do not show the "quick preview", only the "ACCURATE FFMPEG · 360P", and only show the transition (so if transition duration is 5 sec, the example clip should be 5 seconds)
- also give different symbols for the GL transitions.

2026-09-04
- in SAVED IN SQLITE popup, provide a button per entry to delete that entry, also 1 button to delete all.
- advise if we can use gt transitions (https://github.com/scriptituk/xfade-easing#ported-glsl-transitions and https://gl-transitions.com/) to add more transitions. first give options how to implement and what are the different options. goal is to split the final transitions in 2 main groups: 1- based on xfade (these are the current transitions, no change) and 2- GL transitions (these new transitions). I like to have as many transitions as possible (for both groups), so also check if we already have all xfade transitions (give an overview, before implementing the new ones).

2026-09-03
- in photo preview popup window provide functionality to rotate the photo +90 and -90 degrees. this new orientation should also be used in final slideshow result
- in audio preview add possibility to fast forward / reverse the audio track, preferable with drag and drop on a time bar. give options before implementation
- at the end of the slideshow (last photo) make sure the audio is fade out. fade out time to be set by the user via soundtracks pane
- for the individual soundtracks add possibility that user can cut and crop the audio file and add options to fade in and fade out. soundtrack edit to be done in a new popup window.
  keep in mind that total estimate time for tha audio will only user the real audio time and not the total track time. give options for gui and to call up the new popup.
- improvement for Text frame editor: for the background colour on the text frames add possibility to define 2 different colours and to use the existing transitions to go from the first to the second colour. give options for gui.
- add more different fonts for the default text style and text frame. give options and advise which fonts to add.
- in the main header between "storyline" and "soundtrack" add "transitions" to jump to the bottom of the storyline
- add option to normalize the overall audio output in case there are multiple soundtrack but they do not have the same output level. give options for gui.
- disable ken burn effect by default 
- cancel of adding a text frame still adds a text frame, this should be only by save
- add possibility to quickly push a selected photo to a specific place in the storyline. give option for gui.
